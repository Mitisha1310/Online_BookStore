<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <configSections>
    <!-- Add your custom sections here if needed -->
  </configSections>

  <runtime>
    <assemblyBinding xmlns="urn:schemas-microsoft-com:asm.v1">
      <!-- Add any assembly binding redirects or other settings here -->
    </assemblyBinding>
  </runtime>

  <appSettings>
    <!-- Add your application-specific settings here -->
    <add key="Environment" value="QA" />
  </appSettings>

  <connectionStrings>
    <!-- Add database connection strings if needed -->
    <!--<add name="MyDatabase" connectionString="..." providerName="System.Data.SqlClient" />-->
  </connectionStrings>

</configuration>
